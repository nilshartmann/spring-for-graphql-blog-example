type Comment {
    id: ID!

    comment: String!
}

type Post {
    id: ID!

    title: String!
    body: String!
    summary(maxWords: Int! = 5): String!
    comments: [Comment!]!
}

type Query {
    posts: [Post!]!
    post(postId: ID!): Post
}